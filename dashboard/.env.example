# Copy to .env for local dev, or set these in Vercel's Environment
# Variables UI for deployed environments. PUBLIC_ prefix is required by
# SvelteKit to expose a variable to browser-side code.

PUBLIC_AGENT_ENGINE_URL=http://localhost:8002
PUBLIC_GRID_ENGINE_URL=http://localhost:8001
PUBLIC_ORCHESTRATOR_URL=http://localhost:8000
